# ============================================================
# Job Autopilot — Development Dependencies
# ============================================================
# Install with: pip install -r requirements-dev.txt
# (This also installs everything in requirements.txt)

-r requirements.txt

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0

# Linting + formatting (very fast, written in Rust)
ruff>=0.4.0

# Static type checking
mypy>=1.10.0